import { P } from "..";

export default function NextSteps() {
  return (
    <P title="Next steps">
      So far, in this tutorial I mostly touched on the map service. The leave
      lamp as far from being ready. Even if it is more a thought model at some
      point making a real leave lamp, could become a small step. So, I will pick
      up this project in follow-up tutorials that look into other services in
      more detail.
    </P>
  );
}
